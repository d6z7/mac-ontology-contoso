metadata:
  table: sales
  source: Contoso Data Generator V2 (SQLBI, MIT) — parquet, loaded by setup.sh
  schema_version: 0.1.14
  status: draft
  kind: raw_source
  external: true
  observed: '2026-09-18'
  confidence: I
  generated_by: p1_skeleton.py (warehouse catalog)
  notes: 'RAW LANDING / schema-of-record: what arrives, before any modelling. The ontology binds to data/datasets/,
    never to this file. `role: value` on every column is the NEUTRAL physical role and not a ruling —
    which columns carry identity is measured in P2 (mac_admit_identity.py). `values:` domains and every
    count are written by mac_profile.py from a scan; nothing in this file is hand-counted.'
table:
  name: sales
  schema: main
  type: table
  description: Raw landing of data/sales.parquet, loaded verbatim by setup.sh (CREATE TABLE sales AS SELECT
    * FROM read_parquet(...)); contents unmodified. What one row means, and whether this relation is served
    at all, are decided in P2/P3 — not claimed here.
columns:
- name: OrderKey
  type: bigint
  role: composite_key_part
- name: LineNumber
  type: integer
  role: composite_key_part
- name: OrderDate
  type: timestamp
  role: value
- name: DeliveryDate
  type: timestamp
  role: value
- name: CustomerKey
  type: integer
  role: value
- name: StoreKey
  type: integer
  role: value
- name: ProductKey
  type: integer
  role: value
- name: Quantity
  type: integer
  role: value
- name: UnitPrice
  type: decimal(20,5)
  role: value
- name: NetPrice
  type: decimal(20,5)
  role: value
- name: UnitCost
  type: decimal(20,5)
  role: value
- name: CurrencyCode
  type: varchar
  role: value
  values:
  - AUD
  - CAD
  - EUR
  - GBP
  - USD
- name: ExchangeRate
  type: decimal(20,5)
  role: value
