metadata:
  table: v_contoso_fx_rate_day
  source: Contoso Data Generator V2 (SQLBI, MIT) — parquet, loaded by setup.sh
  schema_version: 0.1.14
  status: draft
  kind: served_dataset
  served_kind: fact
  observed: '2026-09-18'
  confidence: I
  generated_by: p3_datasets.py (P3 · dataset-promotion)
  notes: 'SERVED RELATION, passthrough: column-for-column and row-for-row the landing. It is spelled `v_`
    and not `dim_` deliberately — a rate is a measured figure at its own grain, not a label a code is
    given.'
table:
  name: v_contoso_fx_rate_day
  schema: contoso_served
  type: view
  description: 'One row per daily quote for one ordered currency pair (100 450 rows / 100 450 distinct
    keys / 0 duplicates, P2). The grid is COMPLETE: 100 450 = 4 018 days x 5 x 5 (P1 U3, S16), and every
    (order day, order currency) has all 5 quotes out (14 106/14 106, S17), so any conversion the scope''s
    currency question asks is available by join. Served as a relation a question reads at its own grain
    — `Exchange` is a RATE: it may be joined and multiplied, never summed or averaged across days or pairs.'
  confidence: I
columns:
- name: Date
  type: date
  role: composite_key_part
  confidence: I
  notes: 'DATE since 2026-09-19, cast in the transform from the landing''s TIMESTAMP on the operator''s
    ruling; lossless and measured, not assumed — 0 non-midnight over 100 450 of 100 450 rows immediately
    before the cast. main.currencyexchange keeps TIMESTAMP. THE CONVERSION JOIN CROSSES THIS COLUMN: rules.yaml#order_line_usd_conversion
    matches it to v_contoso_order_line.OrderDate on exact equality, and both sides were cast in the same
    change. Re-measured after, identical: 223 974 join rows over 223 974 lines, 0 lines without a quote,
    min 1 / max 1 quotes per line, sum(Quantity * NetPrice * Exchange) = 223 597 710.61.'
- name: FromCurrency
  type: varchar
  role: composite_key_part
  confidence: I
  notes: 5 measured values.
- name: ToCurrency
  type: varchar
  role: composite_key_part
  confidence: I
  notes: 5 measured values.
- name: Exchange
  type: decimal(20,5)
  role: value
  confidence: I
  notes: the rate; exactly 1 on all 20 090 self-pairs (P1 V11), > 0 on every row (P1 V1). NOT additive
    — its additivity class is P8's to register.
foreign_keys:
- name: fk_quote_date
  from_column: Date
  to_table: dim_contoso_calendar_day
  to_column: Date
  enforced: false
  confidence: I
  notes: 0 orphans measured over 100 450 rows (P1 O7)
