# check_mac_public — declared waivers for this bundle.
#
# Format: <path>:<line>:<rule>  <reason>   (two spaces before the reason; a blank reason waives
# nothing). Every waived finding is PRINTED in the verdict and counted, so nothing here is quiet.
#
# Use this only where the inline `mac-public-allow=<rule>` marker CANNOT reach — a line that is
# data, not text. Everywhere else, the inline marker is correct because it sits where a reader
# will see it.

data/samples/customer.src.sample.csv:16:org-name  A vehicle make on one of 20 synthetic rows drawn from the Contoso Data Generator V2 (MIT). The column holds 16 distinct makes over 20 rows and this one appears ONCE, at the same frequency as the three makes beside it — it identifies no estate, and the make is not named here because this file is public and the reason does not need it. Waived rather than edited because the file is generated and its sha256 is recorded in data/samples/samples.run.json: changing a byte falsifies that manifest, and the draw is deterministic so the next ingest restores the row. An inline marker is impossible here — it would land inside the final coordinate field and turn 115.86348 into a non-numeric cell. Waived on the operator's ruling of 2026-09-19.
