suite: mac-ontology-contoso-ontology-generated
version: '1.0'
purpose: DOES THE WAREHOUSE STILL MATCH WHAT THE ONTOLOGY CLAIMS? Every assertion here is RENDERED from
  a concept at run time, never typed — so a concept edit moves its tests with it. The opposite obligation
  to the data-sanity suite, which must not read the declaration at all.
generated: '2026-09-19T00:43:14+02:00'
generated_by: mac_generate_ontology_tests.py/7
engine:
  profile: local
  region: duckdb-local
  workgroup: local
  database: contoso
properties:
- id: O-BRAND-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/brand.yaml
  validates:
  - Brand
  statement: 'Prove Brand''s declared key still identifies one row of contoso_served.dim_contoso_product

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_product by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:brand.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_product.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:brand.grounding.sources.0.key FROM contoso_served.dim_contoso_product GROUP
    BY @cols:concept:brand.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-BRAND-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/brand.yaml
  validates:
  - Brand
  statement: 'Prove every row Brand claims as its own in contoso_served.dim_contoso_product carries a
    usable canonical key

    QUESTION. Of the rows this concept declares to be ITS OWN members, can each one be named by the key
    the concept identifies itself with?

    ANSWER. The concept identifies itself by `Brand` and marks its members by grounding.discriminator:
    Brand.

    RISK. A member whose key is absent or blank is a member this concept cannot name: it still counts
    toward the host relation''s total while answering to no value of the set, so the per-member figures
    and the total disagree and neither looks wrong. The rows the membership declaration EXCLUDES are not
    asserted about here and are not a defect — the concept says such a row is not one of its members.

    ---

    HOW. Counts, among THIS CONCEPT''S OWN members, the rows whose canonical key is NULL or blank after
    trimming. The population is narrowed by the concept''s own membership declaration (grounding.discriminator:
    Brand) rather than taken as the whole relation, because the relation is a HOST that several concepts
    co-inhabit. Blank counts as absent on purpose: narrowing a population must not become a second way
    to spell an absence that escapes the count. Both denominators are returned — the members examined
    and the host relation''s rows — so the narrowing is visible in the run record instead of being taken
    on trust. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_product.Brand, over the rows where "Brand" IS NOT NULL.'
  assertion:
    type: must_be_zero
    columns:
    - member_rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(NULLIF(TRIM(CAST(\"Brand\" AS VARCHAR)), ''))\n           AS member_rows_with_no_identity,\n\
    \       count(*) AS member_rows_examined,\n       (SELECT count(*) FROM contoso_served.dim_contoso_product)\
    \ AS host_relation_rows\nFROM contoso_served.dim_contoso_product\nWHERE \"Brand\" IS NOT NULL"
- id: O-PRODUCT-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/product.yaml
  validates:
  - Product
  statement: 'Prove Product''s declared key still identifies one row of contoso_served.dim_contoso_product

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_product by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:product.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_product.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:product.grounding.sources.0.key FROM contoso_served.dim_contoso_product
    GROUP BY @cols:concept:product.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-PRODUCT-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/product.yaml
  validates:
  - Product
  statement: 'Prove Product''s canonical key is present on every row of contoso_served.dim_contoso_product

    QUESTION. Can every row of this relation be named by the key the concept identifies itself with?

    ANSWER. The concept identifies itself by `ProductKey`.

    RISK. A row whose identity is empty cannot be addressed, compared or joined — it silently drops out
    of every answer that needs it.

    ---

    HOW. Counts empties in the canonical key. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_product.ProductKey.'
  assertion:
    type: must_be_zero
    columns:
    - rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(\"ProductKey\") AS rows_with_no_identity,\n       count(*) AS rows_examined\n\
    FROM contoso_served.dim_contoso_product"
- id: O-PRODUCT_CATEGORY-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/product_category.yaml
  validates:
  - ProductCategory
  statement: 'Prove ProductCategory''s declared key still identifies one row of contoso_served.dim_contoso_product

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_product by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:product_category.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_product.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:product_category.grounding.sources.0.key FROM contoso_served.dim_contoso_product
    GROUP BY @cols:concept:product_category.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-PRODUCT_SUBCATEGORY-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/product_subcategory.yaml
  validates:
  - ProductSubcategory
  statement: 'Prove ProductSubcategory''s declared key still identifies one row of contoso_served.dim_contoso_product

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_product by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:product_subcategory.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_product.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:product_subcategory.grounding.sources.0.key FROM contoso_served.dim_contoso_product
    GROUP BY @cols:concept:product_subcategory.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-AGE_BAND-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/age_band.yaml
  validates:
  - AgeBand
  statement: 'Prove AgeBand''s declared key still identifies one row of contoso_served.dim_contoso_customer

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_customer by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:age_band.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:age_band.grounding.sources.0.key FROM contoso_served.dim_contoso_customer
    GROUP BY @cols:concept:age_band.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-CUSTOMER-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/customer.yaml
  validates:
  - Customer
  statement: 'Prove Customer''s declared key still identifies one row of contoso_served.dim_contoso_customer

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_customer by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:customer.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:customer.grounding.sources.0.key FROM contoso_served.dim_contoso_customer
    GROUP BY @cols:concept:customer.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-CUSTOMER-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/customer.yaml
  validates:
  - Customer
  statement: 'Prove Customer''s canonical key is present on every row of contoso_served.dim_contoso_customer

    QUESTION. Can every row of this relation be named by the key the concept identifies itself with?

    ANSWER. The concept identifies itself by `CustomerKey`.

    RISK. A row whose identity is empty cannot be addressed, compared or joined — it silently drops out
    of every answer that needs it.

    ---

    HOW. Counts empties in the canonical key. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.CustomerKey.'
  assertion:
    type: must_be_zero
    columns:
    - rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(\"CustomerKey\") AS rows_with_no_identity,\n       count(*) AS rows_examined\n\
    FROM contoso_served.dim_contoso_customer"
- id: O-CURRENCY-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/currency.yaml
  validates:
  - Currency
  statement: 'Prove Currency''s declared key still identifies one row of contoso_served.v_contoso_order_line

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 2 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.v_contoso_order_line by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:currency.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_order_line.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:currency.grounding.sources.0.key FROM contoso_served.v_contoso_order_line
    GROUP BY @cols:concept:currency.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-CURRENCY-KEY1
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/currency.yaml
  validates:
  - Currency
  statement: 'Prove Currency''s declared key still identifies one row of contoso_served.v_contoso_fx_rate_day

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 3 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.v_contoso_fx_rate_day by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:currency.grounding.sources.1.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_fx_rate_day.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:currency.grounding.sources.1.key FROM contoso_served.v_contoso_fx_rate_day
    GROUP BY @cols:concept:currency.grounding.sources.1.key HAVING count(*) > 1)'
- id: O-CURRENCY-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/currency.yaml
  validates:
  - Currency
  statement: 'Prove every row Currency claims as its own in contoso_served.v_contoso_order_line carries
    a usable canonical key

    QUESTION. Of the rows this concept declares to be ITS OWN members, can each one be named by the key
    the concept identifies itself with?

    ANSWER. The concept identifies itself by `CurrencyCode` and marks its members by grounding.discriminator:
    CurrencyCode.

    RISK. A member whose key is absent or blank is a member this concept cannot name: it still counts
    toward the host relation''s total while answering to no value of the set, so the per-member figures
    and the total disagree and neither looks wrong. The rows the membership declaration EXCLUDES are not
    asserted about here and are not a defect — the concept says such a row is not one of its members.

    ---

    HOW. Counts, among THIS CONCEPT''S OWN members, the rows whose canonical key is NULL or blank after
    trimming. The population is narrowed by the concept''s own membership declaration (grounding.discriminator:
    CurrencyCode) rather than taken as the whole relation, because the relation is a HOST that several
    concepts co-inhabit. Blank counts as absent on purpose: narrowing a population must not become a second
    way to spell an absence that escapes the count. Both denominators are returned — the members examined
    and the host relation''s rows — so the narrowing is visible in the run record instead of being taken
    on trust. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_order_line.CurrencyCode, over the rows where "CurrencyCode" IS NOT
    NULL.'
  assertion:
    type: must_be_zero
    columns:
    - member_rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(NULLIF(TRIM(CAST(\"CurrencyCode\" AS VARCHAR)), ''))\n           AS member_rows_with_no_identity,\n\
    \       count(*) AS member_rows_examined,\n       (SELECT count(*) FROM contoso_served.v_contoso_order_line)\
    \ AS host_relation_rows\nFROM contoso_served.v_contoso_order_line\nWHERE \"CurrencyCode\" IS NOT NULL"
- id: O-EXCHANGE_RATE-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/exchange_rate.yaml
  validates:
  - ExchangeRate
  statement: 'Prove ExchangeRate''s declared key still identifies one row of contoso_served.v_contoso_fx_rate_day

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 3 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.v_contoso_fx_rate_day by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:exchange_rate.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_fx_rate_day.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:exchange_rate.grounding.sources.0.key FROM contoso_served.v_contoso_fx_rate_day
    GROUP BY @cols:concept:exchange_rate.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-GROSS_SALES_AMOUNT-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/gross_sales_amount.yaml
  validates:
  - GrossSalesAmount
  statement: 'Prove GrossSalesAmount''s declared key still identifies one row of contoso_served.v_contoso_order_line

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 2 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.v_contoso_order_line by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:gross_sales_amount.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_order_line.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:gross_sales_amount.grounding.sources.0.key FROM contoso_served.v_contoso_order_line
    GROUP BY @cols:concept:gross_sales_amount.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-NET_SALES_AMOUNT-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/net_sales_amount.yaml
  validates:
  - NetSalesAmount
  statement: 'Prove NetSalesAmount''s declared key still identifies one row of contoso_served.v_contoso_order_line

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 2 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.v_contoso_order_line by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:net_sales_amount.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_order_line.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:net_sales_amount.grounding.sources.0.key FROM contoso_served.v_contoso_order_line
    GROUP BY @cols:concept:net_sales_amount.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-CONTINENT-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/continent.yaml
  validates:
  - Continent
  statement: 'Prove Continent''s declared key still identifies one row of contoso_served.dim_contoso_customer

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_customer by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:continent.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:continent.grounding.sources.0.key FROM contoso_served.dim_contoso_customer
    GROUP BY @cols:concept:continent.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-CONTINENT-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/continent.yaml
  validates:
  - Continent
  statement: 'Prove every row Continent claims as its own in contoso_served.dim_contoso_customer carries
    a usable canonical key

    QUESTION. Of the rows this concept declares to be ITS OWN members, can each one be named by the key
    the concept identifies itself with?

    ANSWER. The concept identifies itself by `Continent` and marks its members by grounding.discriminator:
    Continent.

    RISK. A member whose key is absent or blank is a member this concept cannot name: it still counts
    toward the host relation''s total while answering to no value of the set, so the per-member figures
    and the total disagree and neither looks wrong. The rows the membership declaration EXCLUDES are not
    asserted about here and are not a defect — the concept says such a row is not one of its members.

    ---

    HOW. Counts, among THIS CONCEPT''S OWN members, the rows whose canonical key is NULL or blank after
    trimming. The population is narrowed by the concept''s own membership declaration (grounding.discriminator:
    Continent) rather than taken as the whole relation, because the relation is a HOST that several concepts
    co-inhabit. Blank counts as absent on purpose: narrowing a population must not become a second way
    to spell an absence that escapes the count. Both denominators are returned — the members examined
    and the host relation''s rows — so the narrowing is visible in the run record instead of being taken
    on trust. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.Continent, over the rows where "Continent" IS NOT NULL.'
  assertion:
    type: must_be_zero
    columns:
    - member_rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(NULLIF(TRIM(CAST(\"Continent\" AS VARCHAR)), ''))\n           AS member_rows_with_no_identity,\n\
    \       count(*) AS member_rows_examined,\n       (SELECT count(*) FROM contoso_served.dim_contoso_customer)\
    \ AS host_relation_rows\nFROM contoso_served.dim_contoso_customer\nWHERE \"Continent\" IS NOT NULL"
- id: O-COUNTRY-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/country.yaml
  validates:
  - Country
  statement: 'Prove Country''s declared key still identifies one row of contoso_served.dim_contoso_customer

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_customer by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:country.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:country.grounding.sources.0.key FROM contoso_served.dim_contoso_customer
    GROUP BY @cols:concept:country.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-COUNTRY-KEY1
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/country.yaml
  validates:
  - Country
  statement: 'Prove Country''s declared key still identifies one row of contoso_served.dim_contoso_store

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_store by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:country.grounding.sources.1.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_store.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:country.grounding.sources.1.key FROM contoso_served.dim_contoso_store GROUP
    BY @cols:concept:country.grounding.sources.1.key HAVING count(*) > 1)'
- id: O-COUNTRY-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/country.yaml
  validates:
  - Country
  statement: 'Prove every row Country claims as its own in contoso_served.dim_contoso_customer carries
    a usable canonical key

    QUESTION. Of the rows this concept declares to be ITS OWN members, can each one be named by the key
    the concept identifies itself with?

    ANSWER. The concept identifies itself by `Country` and marks its members by grounding.discriminator:
    Country.

    RISK. A member whose key is absent or blank is a member this concept cannot name: it still counts
    toward the host relation''s total while answering to no value of the set, so the per-member figures
    and the total disagree and neither looks wrong. The rows the membership declaration EXCLUDES are not
    asserted about here and are not a defect — the concept says such a row is not one of its members.

    ---

    HOW. Counts, among THIS CONCEPT''S OWN members, the rows whose canonical key is NULL or blank after
    trimming. The population is narrowed by the concept''s own membership declaration (grounding.discriminator:
    Country) rather than taken as the whole relation, because the relation is a HOST that several concepts
    co-inhabit. Blank counts as absent on purpose: narrowing a population must not become a second way
    to spell an absence that escapes the count. Both denominators are returned — the members examined
    and the host relation''s rows — so the narrowing is visible in the run record instead of being taken
    on trust. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.Country, over the rows where "Country" IS NOT NULL.'
  assertion:
    type: must_be_zero
    columns:
    - member_rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(NULLIF(TRIM(CAST(\"Country\" AS VARCHAR)), ''))\n           AS member_rows_with_no_identity,\n\
    \       count(*) AS member_rows_examined,\n       (SELECT count(*) FROM contoso_served.dim_contoso_customer)\
    \ AS host_relation_rows\nFROM contoso_served.dim_contoso_customer\nWHERE \"Country\" IS NOT NULL"
- id: O-GEO_AREA-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/geo_area.yaml
  validates:
  - GeoArea
  statement: 'Prove GeoArea''s declared key still identifies one row of contoso_served.dim_contoso_customer

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_customer by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:geo_area.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:geo_area.grounding.sources.0.key FROM contoso_served.dim_contoso_customer
    GROUP BY @cols:concept:geo_area.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-GEO_AREA-KEY1
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/geo_area.yaml
  validates:
  - GeoArea
  statement: 'Prove GeoArea''s declared key still identifies one row of contoso_served.dim_contoso_store

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_store by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:geo_area.grounding.sources.1.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_store.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:geo_area.grounding.sources.1.key FROM contoso_served.dim_contoso_store
    GROUP BY @cols:concept:geo_area.grounding.sources.1.key HAVING count(*) > 1)'
- id: O-GEO_AREA-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/geo_area.yaml
  validates:
  - GeoArea
  statement: 'Prove GeoArea''s canonical key is present on every row of contoso_served.dim_contoso_customer

    QUESTION. Can every row of this relation be named by the key the concept identifies itself with?

    ANSWER. The concept identifies itself by `GeoAreaKey`.

    RISK. A row whose identity is empty cannot be addressed, compared or joined — it silently drops out
    of every answer that needs it.

    ---

    HOW. Counts empties in the canonical key. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_customer.GeoAreaKey.'
  assertion:
    type: must_be_zero
    columns:
    - rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(\"GeoAreaKey\") AS rows_with_no_identity,\n       count(*) AS rows_examined\n\
    FROM contoso_served.dim_contoso_customer"
- id: O-ORDER_LINE-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/order_line.yaml
  validates:
  - OrderLine
  statement: 'Prove OrderLine''s declared key still identifies one row of contoso_served.v_contoso_order_line

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 2 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.v_contoso_order_line by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:order_line.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.v_contoso_order_line.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:order_line.grounding.sources.0.key FROM contoso_served.v_contoso_order_line
    GROUP BY @cols:concept:order_line.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-STORE-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/store.yaml
  validates:
  - Store
  statement: 'Prove Store''s declared key still identifies one row of contoso_served.dim_contoso_store

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_store by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:store.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_store.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:store.grounding.sources.0.key FROM contoso_served.dim_contoso_store GROUP
    BY @cols:concept:store.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-STORE-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/store.yaml
  validates:
  - Store
  statement: 'Prove Store''s canonical key is present on every row of contoso_served.dim_contoso_store

    QUESTION. Can every row of this relation be named by the key the concept identifies itself with?

    ANSWER. The concept identifies itself by `StoreKey`.

    RISK. A row whose identity is empty cannot be addressed, compared or joined — it silently drops out
    of every answer that needs it.

    ---

    HOW. Counts empties in the canonical key. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_store.StoreKey.'
  assertion:
    type: must_be_zero
    columns:
    - rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(\"StoreKey\") AS rows_with_no_identity,\n       count(*) AS rows_examined\n\
    FROM contoso_served.dim_contoso_store"
- id: O-STORE_STATUS-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/store_status.yaml
  validates:
  - StoreStatus
  statement: 'Prove StoreStatus''s declared key still identifies one row of contoso_served.dim_contoso_store

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_store by the DECLARED key and counts the groups holding more
    than one row. The column list is READ from the concept at run time (@cols:concept:store_status.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_store.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:store_status.grounding.sources.0.key FROM contoso_served.dim_contoso_store
    GROUP BY @cols:concept:store_status.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-STORE_STATUS-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/store_status.yaml
  validates:
  - StoreStatus
  statement: 'Prove every row StoreStatus claims as its own in contoso_served.dim_contoso_store carries
    a usable canonical key

    QUESTION. Of the rows this concept declares to be ITS OWN members, can each one be named by the key
    the concept identifies itself with?

    ANSWER. The concept identifies itself by `Status` and marks its members by grounding.discriminator:
    Status.

    RISK. A member whose key is absent or blank is a member this concept cannot name: it still counts
    toward the host relation''s total while answering to no value of the set, so the per-member figures
    and the total disagree and neither looks wrong. The rows the membership declaration EXCLUDES are not
    asserted about here and are not a defect — the concept says such a row is not one of its members.

    ---

    HOW. Counts, among THIS CONCEPT''S OWN members, the rows whose canonical key is NULL or blank after
    trimming. The population is narrowed by the concept''s own membership declaration (grounding.discriminator:
    Status) rather than taken as the whole relation, because the relation is a HOST that several concepts
    co-inhabit. Blank counts as absent on purpose: narrowing a population must not become a second way
    to spell an absence that escapes the count. Both denominators are returned — the members examined
    and the host relation''s rows — so the narrowing is visible in the run record instead of being taken
    on trust. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_store.Status, over the rows where "Status" IS NOT NULL.'
  assertion:
    type: must_be_zero
    columns:
    - member_rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(NULLIF(TRIM(CAST(\"Status\" AS VARCHAR)), ''))\n           AS member_rows_with_no_identity,\n\
    \       count(*) AS member_rows_examined,\n       (SELECT count(*) FROM contoso_served.dim_contoso_store)\
    \ AS host_relation_rows\nFROM contoso_served.dim_contoso_store\nWHERE \"Status\" IS NOT NULL"
- id: O-CALENDAR_DAY-KEY
  family: key_grain
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/calendar_day.yaml
  validates:
  - CalendarDay
  statement: 'Prove CalendarDay''s declared key still identifies one row of contoso_served.dim_contoso_calendar_day

    QUESTION. Does the key this concept says it is identified by actually pick out a single row?

    ANSWER. The concept declares 1 column(s).

    RISK. If the key repeats, every join through this concept fans out and every figure downstream is
    multiplied without anything looking wrong.

    ---

    HOW. Groups contoso_served.dim_contoso_calendar_day by the DECLARED key and counts the groups holding
    more than one row. The column list is READ from the concept at run time (@cols:concept:calendar_day.grounding.sources.0.key),
    never typed here — a typed key would go on passing after the concept changed. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_calendar_day.'
  assertion:
    type: must_be_zero
    columns:
    - keys_holding_more_than_one_row
  tolerance: 0
  sql: 'SELECT count(*) AS keys_holding_more_than_one_row

    FROM (SELECT @cols:concept:calendar_day.grounding.sources.0.key FROM contoso_served.dim_contoso_calendar_day
    GROUP BY @cols:concept:calendar_day.grounding.sources.0.key HAVING count(*) > 1)'
- id: O-CALENDAR_DAY-IDENT
  family: identity
  test_kind: mac.test_kind.conformance
  severity: major
  source: ontology/concepts/calendar_day.yaml
  validates:
  - CalendarDay
  statement: 'Prove CalendarDay''s canonical key is present on every row of contoso_served.dim_contoso_calendar_day

    QUESTION. Can every row of this relation be named by the key the concept identifies itself with?

    ANSWER. The concept identifies itself by `Date`.

    RISK. A row whose identity is empty cannot be addressed, compared or joined — it silently drops out
    of every answer that needs it.

    ---

    HOW. Counts empties in the canonical key. GENERATED by mac_generate_ontology_tests.py/7.

    WHERE. contoso_served.dim_contoso_calendar_day.Date.'
  assertion:
    type: must_be_zero
    columns:
    - rows_with_no_identity
  tolerance: 0
  sql: "SELECT count(*) - count(\"Date\") AS rows_with_no_identity,\n       count(*) AS rows_examined\n\
    FROM contoso_served.dim_contoso_calendar_day"
