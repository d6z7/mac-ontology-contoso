id: RC10
captured_at: '2026-09-23T21:36:46+00:00'
ontology_fingerprint: b6a1acf2f00b6890
acceptance_fingerprint: 1cfcd2a8d6f5f5e5
engine:
  provider: bedrock
  model: global.anthropic.claude-opus-4-6-v1
  effort: max
  mode: llm-execute
  athena: true
route: wiki+athena
answer: Order, as a single figure.
answer_parts:
  value: 15899
  unit: null
  measure: Order
  reading: '"How many orders" means COUNT(DISTINCT OrderKey) over the lines in scope,
    by OrderDate. An order is counted once however many lines it carries, and the
    answer discloses the line count beside it when the question could have meant either.'
  table: null
  status: ok
  confidence: I
  assumptions:
  - kind: dq
    text: 'An order agrees with itself on customer, store, order date, currency and
      delivery date (Measured 0 of 93 470 orders on 2026-09-24, on every one of the
      five axes independently. This is the whole basis for declaring Order an event
      rather than a derived grouping: it is atomic in the data. A non-zero here means
      the concept''s grain claim is false and the rules above must be re-read before
      anything is answered from them.)'
    ref: concept://example/contoso/Order
  - kind: dq
    text: Every served line belongs to exactly one order (Measured 0 nulls over 223
      974 rows on 2026-09-24. A null OrderKey would be a line no order owns, and the
      distinct count would silently drop it.)
    ref: concept://example/contoso/Order
  - kind: dq
    text: counted by OrderKey (identity.canonical_key of Order)
    ref: null
  concepts:
  - concept: Order
    label: Order
    ref: concept://example/contoso/Order
  - concept: OrderLine
    label: Order Line
    ref: concept://example/contoso/OrderLine
  rules: []
  edges: []
  intent:
    subject: Order
    operation: count
    slices: []
    filters: []
    join_filters: []
    period:
      start: '2024-01-01'
      end: '2025-01-01'
      raw: '2024'
    ordering: null
    limit: null
    denominator: null
    stage: null
    confidence: 0.95
  raw: 'answer_id=UUID(''024e4bb7-aa70-4d3b-91a7-5eeca251f2ba'') pack=PackIdentifier(name=''example/contoso'',
    version=''live'') question=''How many orders were placed in 2024?'' intent=Intent(subject=''Order'',
    operation=<OperationKind.COUNT: ''count''>, slices=[], filters=[], join_filters=[],
    period=PeriodSpec(start=datetime.date(2024, 1, 1), end=datetime.date(2025, 1,
    1), raw=''2024''), ordering=None, limit=None, denominator=None, stage=None, confidence=0.95)
    status=''ok'' value=15899 unit=None semantics_applied=SemanticsApplied(measure=''Order'',
    reading=''"How many orders" means COUNT(DISTINCT OrderKey) over the lines in scope,
    by OrderDate. An order is counted once however many lines it carries, and the
    answer discloses the line count beside it when the question could have meant either.'',
    definitions=[DefinitionRef(concept=''Order'', ref=''concept://example/contoso/Order''),
    DefinitionRef(concept=''OrderLine'', ref=''concept://example/contoso/OrderLine'')],
    rules=[], edges=[]) caveats=[Caveat(kind=<CaveatKind.DQ: ''dq''>, note="An order
    agrees with itself on customer, store, order date, currency and delivery date
    (Measured 0 of 93 470 orders on 2026-09-24, on every one of the five axes independently.
    This is the whole basis for declaring Order an event rather than a derived grouping:
    it is atomic in the data. A non-zero here means the concept''s grain claim is
    false and the rules above must be re-read before anything is answered from them.)",
    ref=''concept://example/contoso/Order''), Caveat(kind=<CaveatKind.DQ: ''dq''>,
    note=''Every served line belongs to exactly one order (Measured 0 nulls over 223
    974 rows on 2026-09-24. A null OrderKey would be a line no order owns, and the
    distinct count would silently drop it.)'', ref=''concept://example/contoso/Order''),
    Caveat(kind=<CaveatKind.DQ: ''dq''>, note=''counted by OrderKey (identity.canonical_key
    of Order)'', ref=None)] confidence=<ConfidenceTier.INFERRED: ''I''> provenance=Provenance(sql=''SELECT
    COUNT(DISTINCT v_contoso_order_line.OrderKey) AS order\nFROM contoso_served.v_contoso_order_line\nWHERE
    v_contoso_order_line.OrderDate >= :period_start AND v_contoso_order_line.OrderDate
    < :period_end'', params={''period_start'': datetime.date(2024, 1, 1), ''period_end'':
    datetime.date(2025, 1, 1)}, adapter=AdapterProvenance(kind=''duckdb'', engine_query_id=''duckdb-5'',
    bytes_scanned=0), views=[''contoso_served.v_contoso_order_line''], executed_at=datetime.datetime(2026,
    9, 23, 21, 36, 46, 546029, tzinfo=datetime.timezone.utc), runtime_version=''0.1.0'')'
sql:
- 'SELECT COUNT(DISTINCT v_contoso_order_line.OrderKey) AS order

  FROM contoso_served.v_contoso_order_line

  WHERE v_contoso_order_line.OrderDate >= :period_start AND v_contoso_order_line.OrderDate
  < :period_end'
sql_valid: null
sql_errors: []
disclosures: []
result:
  value: 15899.0
  cols:
  - Order
  rows: 1
  sample_rows:
  - - 15899
  status: ok
error: null
