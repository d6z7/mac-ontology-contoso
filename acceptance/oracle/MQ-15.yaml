question:
  id: MQ-15
  text: What is the average customer age?
  class: SHAPE
  expected_outcome: REFUSE
kind: shape
authority: derived
authority_note: 'DERIVED FROM THE QUESTION TEXT by genoracles.py, and from nothing else. The outcome comes
  from the corpus''s own regression tag; the pinned columns from values the question names that a DECLARED
  register resolves, plus the reporting period when the question names one; the shape prohibitions from
  the question''s own grammar (how-many vs which, and an explicit top-N). NO EXPECTED VALUE IS ASSERTED
  and none may be added here: this bundle has no second source of truth for what a measure means, so a
  value written from the ontology would test the planner against the ontology and not against the world.
  NOTHING WAS READ FROM A RUN. These assertions were not observed, so a capture that disagrees is evidence
  about the engine, not about this file.'
expected:
  outcome: REFUSE
  shape: refusal
derivation:
  outcome: the corpus tags this 'edge-case-unanswerable'
  pins: []
  shape: []
about: []
stable: true
